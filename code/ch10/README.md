# ch10

第10章の実験コードを置く。

## sec10_01_transpile_intro.py

トランスパイルにより、同じ論理回路が結合制約やネイティブゲートに合わせて変形される様子を確認する。

実行例は次のとおりである。

```bash
python sec10_01_transpile_intro.py --outdir out --datadir data
```

図は `out/` に、数値データは `data/` に保存される。

