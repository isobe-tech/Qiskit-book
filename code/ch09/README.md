# ch09

第9章の実験コードを置く。

## sec09_01_statevector_practice.py

statevectorを使い、同じ確率でも状態が違い得ることを確認する。

実行例は次のとおりである。

```bash
python sec09_01_statevector_practice.py --outdir out --datadir data
```

図は `out/` に、数値データは `data/` に保存される。

