# ch08

第8章の実験コードを置く。

## sec08_01_entanglement.py

ベル状態 Φ+ と Φ- を作り、計算基底とX基底で測定した結果を比較する。

実行例は次のとおりである。

```bash
python sec08_01_entanglement.py --outdir out --datadir data
```

図は `out/` に、数値データは `data/` に保存される。
