# ch06

第6章の実験コードを置く。

## sec06_01_tensor_product.py

2量子ビットの回路を二つ用意し、理論確率と有限ショットの観測結果を並べてテンソル積の直感を固める。

実行例は次のとおりである。

```bash
python sec06_01_tensor_product.py --outdir out --datadir data
```

図は `out/` に、数値データは `data/` に保存される。

