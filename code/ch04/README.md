# ch04

第4章の実験コードを置く。

## sec04_01_z_and_phase.py

Zゲートが計算基底では確率を変えない一方で、基底を変えると確率へ現れることを確認する。
位相 $\phi$ を連続的に動かしたときの確率の変化も可視化する。

実行例は次のとおりである。

```bash
python sec04_01_z_and_phase.py --outdir out --datadir data
```

図は `out/` に、数値データは `data/` に保存される。

